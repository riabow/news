<%= form_for(sp2) do |f| %>
  <% if sp2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sp2.errors.count, "error") %> prohibited this sp2 from being saved:</h2>

      <ul>
      <% sp2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
