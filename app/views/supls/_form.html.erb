<%= form_for(supl) do |f| %>
  <% if supl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supl.errors.count, "error") %> prohibited this supl from being saved:</h2>

      <ul>
      <% supl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
